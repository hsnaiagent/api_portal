# Server-side Gemini API key (preferred — never exposed to the browser).
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Legacy client-side name (optional fallback for local dev only).
# VITE_GEMINI_API_KEY=
